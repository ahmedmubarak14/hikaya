import { Module } from '@nestjs/common';

/**
 * Studios module — Phase 1 placeholder.
 *
 * Domain logic to be implemented per the PRD: see README and the corresponding
 * Prisma model(s). This stub exists so feature work can be added incrementally
 * without restructuring the AppModule.
 */
@Module({})
export class StudiosModule {}
